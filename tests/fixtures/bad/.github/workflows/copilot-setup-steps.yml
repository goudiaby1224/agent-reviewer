name: Copilot Setup Steps
jobs:
  copilot-setup-steps:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        x: [1]
    steps:
      - uses: actions/checkout@v4
  other:
    runs-on: ubuntu-latest
    steps:
      - run: echo hi
